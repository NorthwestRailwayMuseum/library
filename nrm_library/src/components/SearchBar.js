import React from 'react';
import TextField from '@mui/material/TextField';

function SearchBar() {
    return (
        <TextField fullWidth label="Search Books" variant="outlined" />
    );
}

export default SearchBar;